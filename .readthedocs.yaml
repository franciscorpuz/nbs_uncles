version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    # Installs your package + NumPy so Sphinx can import it safely
    - method: pip
      path: .
    # Installs Sphinx, the theme, and MyST-NB
    - requirements: docs/requirements.txt
